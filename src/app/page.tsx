export default function HomePage() {
  return (
    <main style={{ fontFamily: "sans-serif", padding: "2rem" }}>
      <h1>TriGuard</h1>
      <p>Input layer MVP — TriGuard, TaskSnap, InboxZero.</p>
      <p>
        Check <code>/api/health</code> to confirm the server is running.
      </p>
    </main>
  );
}